enum ENDPOINTS {
  LINES = '/lines',
  FORECLOSURES = '/foreclosures',
  ALBUMS = '/albums',
  LOGIN = '/auth/login',
  REFRESH = '/auth/refresh',
  CHECK = '/me',
  LOGOUT = '/auth/logout',
}

export default ENDPOINTS;
